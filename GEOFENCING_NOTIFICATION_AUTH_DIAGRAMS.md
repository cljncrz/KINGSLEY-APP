# Geofencing Notification Auth - Visual Diagrams

## 1. Complete User Journey - Logged In User

```
┌─────────────────────────────────────────────────────────────────────┐
│                         LOGGED-IN USER FLOW                         │
└─────────────────────────────────────────────────────────────────────┘

    User Logged In
    (currentUser != null)
           │
           ↓
    ┌──────────────────┐
    │  App Running     │
    │ (Any State)      │
    └────────┬─────────┘
             │
             ↓
    ┌──────────────────────────┐
    │ Backend Detects Geofence │
    │  (User Near Carwash)     │
    └────────┬─────────────────┘
             │
             ↓
    ┌──────────────────────────────────────┐
    │  FCM Notification Sent to Device     │
    │  data: { type: "geofence" }          │
    │  title: "You're near a carwash!"     │
    └────────┬─────────────────────────────┘
             │
             ↓
    ┌──────────────────────────┐
    │  Local Notification      │
    │  Displayed on Device     │
    └────────┬─────────────────┘
             │
             ↓
    ┌──────────────────────────┐
    │  👆 USER TAPS NOTIFICATION│
    └────────┬─────────────────┘
             │
             ├─ App in Foreground? 
             │  └─→ onMessage listener
             │
             ├─ App in Background?
             │  └─→ onMessageOpenedApp listener
             │
             └─ App Terminated?
                └─→ getInitialMessage() handler
                    └─→ App relaunches
                    
             ↓
    ┌───────────────────────────────────────┐
    │  Check: _isUserAuthenticated()        │
    │  if (FirebaseAuth.currentUser != null)│
    └────────┬────────────────────────────────┘
             │
             ↓ (TRUE - User logged in)
             
    ┌──────────────────────────────────┐
    │  Get.toNamed('/geofence-status') │
    │  Navigate to Geofence Screen     │
    └────────┬─────────────────────────┘
             │
             ↓
    ┌──────────────────────────────────────┐
    │  ✅ SUCCESS                           │
    │  GeofenceStatusScreen Opens          │
    │  User Sees:                          │
    │  • Location status (near/far)        │
    │  • Distance to carwash              │
    │  • Monitoring controls              │
    │  • Get Directions button            │
    └──────────────────────────────────────┘
```

## 2. Complete User Journey - Non-Logged-In User

```
┌────────────────────────────────────────────────────────────────────────┐
│                    NON-LOGGED-IN USER FLOW                             │
└────────────────────────────────────────────────────────────────────────┘

    No User Logged In
    (currentUser == null)
           │
           ↓
    ┌──────────────────────┐
    │  App Installed       │
    │  But Not Logged In   │
    └────────┬─────────────┘
             │
             ↓
    ┌──────────────────────────┐
    │ Backend Detects Geofence │
    │  (Device Near Carwash)   │
    └────────┬─────────────────┘
             │
             ↓
    ┌──────────────────────────────────────┐
    │  FCM Notification Sent to All Users  │
    │  (Regardless of login status)        │
    │  data: { type: "geofence" }          │
    │  title: "You're near a carwash!"     │
    └────────┬──────────────────────────────┘
             │
             ↓
    ┌──────────────────────────────┐
    │  Local Notification in       │
    │  Notification Center         │
    │  (No app action until tap)   │
    └────────┬─────────────────────┘
             │
             ↓
    ┌──────────────────────────┐
    │  👆 USER TAPS NOTIFICATION│
    └────────┬─────────────────┘
             │
             ├─ App in Background?
             │  └─→ onMessageOpenedApp listener
             │
             └─ App Terminated?
                └─→ App relaunches
                └─→ getInitialMessage() handler
                    
             ↓
    ┌───────────────────────────────────────┐
    │  Check: _isUserAuthenticated()        │
    │  if (FirebaseAuth.currentUser == null)│
    └────────┬────────────────────────────────┘
             │
             ↓ (FALSE - Not logged in)
             
    ┌──────────────────────────────────────────┐
    │  🔔 Show Snackbar Message                │
    │  "Login Required"                        │
    │  "Please login or sign up to view        │
    │   geofencing information."               │
    │  Position: Top of Screen                 │
    │  Duration: 4 seconds                     │
    └────────┬─────────────────────────────────┘
             │
             ↓ (Simultaneously)
             
    ┌──────────────────────────────────────┐
    │  Get.toNamed('/login')               │
    │  Navigate to Login/Signup Screen     │
    └────────┬─────────────────────────────┘
             │
             ↓
    ┌────────────────────────────────────────┐
    │  Login Screen Opens                    │
    │  User sees:                            │
    │  • Login form                          │
    │  • Signup link/button                  │
    │  • Other auth options                  │
    └────────┬───────────────────────────────┘
             │
             ├─ User enters credentials
             │  │
             │  ↓
             │  Firebase Auth validates
             │  │
             │  ↓
             └─ Auth state updates
                (currentUser = User object)
                
             ↓
    ┌──────────────────────────────────────────┐
    │  ✅ LOGIN SUCCESSFUL                      │
    │  User Now Has Account                    │
    │  Next Geofence Notification:             │
    │  • User receives notification            │
    │  • Taps notification                     │
    │  • Auth check PASSES ✓                   │
    │  • Opens Geofence Status Screen          │
    └──────────────────────────────────────────┘
```

## 3. Authentication Check Decision Tree

```
                    Notification Received
                           │
                           ↓
                    Is it a Geofence?
                    ↙                ↘
                  YES                 NO
                   │                  │
                   ↓                  ↓
          Call _handleGeofence   Check for other
          Notification()         notification types
                   │                  │
                   ↓                  └─→ [Not handled by this flow]
          
          _isUserAuthenticated()
          Check: currentUser != null
                   │
              ┌────┴────┐
              │          │
             YES         NO
              │          │
              ↓          ↓
        Get.toNamed   Get.snackbar()
        ('/geofence-  ('Login Required')
         status')      │
              │        ↓
              │     Get.toNamed
              │     ('/login')
              │        │
              ↓        ↓
        ┌─────────┐ ┌──────────┐
        │Geofence │ │  Login   │
        │ Status  │ │ Screen   │
        │ Screen  │ │ Opens    │
        │ Opens   │ │          │
        └─────────┘ └──────────┘
```

## 4. State Diagram - App States

```
        ╔════════════════════════════════════════════════════════════╗
        ║              NOTIFICATION HANDLING BY APP STATE             ║
        ╚════════════════════════════════════════════════════════════╝

┌──────────────────────────┐   ┌──────────────────────────┐
│      FOREGROUND STATE    │   │     BACKGROUND STATE     │
│ (App is open and active) │   │ (App minimized/inactive) │
├──────────────────────────┤   ├──────────────────────────┤
│ Notification Arrives     │   │ Notification Arrives     │
│         ↓                │   │         ↓                │
│ onMessage listener fires │   │ Notification shown in    │
│         ↓                │   │ notification center      │
│ Banner/Alert displayed   │   │         ↓                │
│         ↓                │   │ User taps notification   │
│ User sees immediately    │   │         ↓                │
│ May tap notification or  │   │ onMessageOpenedApp fires │
│ continue using app       │   │         ↓                │
│                          │   │ Navigation proceeds      │
│ Listener: onMessage      │   │                          │
│                          │   │ Listener: onMessageOpenedApp
└──────────────────────────┘   └──────────────────────────┘

┌──────────────────────────┐   ┌──────────────────────────┐
│      TERMINATED STATE    │   │    TRANSITION STATES    │
│  (App completely closed) │   │ (Switching between any  │
├──────────────────────────┤   │  of the above)          │
│ Notification Arrives     │   ├──────────────────────────┤
│         ↓                │   │ Quick transitions don't  │
│ Stored in notification   │   │ affect notification      │
│ center (OS level)        │   │ handling                 │
│         ↓                │   │                          │
│ User taps from lock      │   │ Persistent listeners     │
│ screen or notification   │   │ continue to work         │
│ center                   │   │                          │
│         ↓                │   │ Navigation executes      │
│ App relaunches           │   │ based on current state   │
│         ↓                │   │                          │
│ getInitialMessage() gets │   │ Listener: Depends on    │
│ notification data        │   │ where notification was   │
│         ↓                │   │ tapped from              │
│ Navigation proceeds      │   │                          │
│                          │   │                          │
│ Listener: getInitialMessage()│                          │
└──────────────────────────┘   └──────────────────────────┘
```

## 5. Payload Structure Diagram

```
┌─────────────────────────────────────────────────────────────┐
│              NOTIFICATION PAYLOAD STRUCTURE                 │
└─────────────────────────────────────────────────────────────┘

                         Notification
                              │
                ┌─────────────┴──────────────┐
                │                            │
           notification              data (optional)
          (required)                        │
                │                ┌──────────┴──────────┐
                │                │                     │
          ┌─────┴─────┐      type         other fields
          │            │      │           (locationId,
       title        body   "geofence"      distance,
       (required)  (required) or           etc.)
                             "geofencing"

Example Payload:
{
  "notification": {
    "title": "You're near a Kingsley Carwash!",
    "body": "Check out special offers"
  },
  "data": {
    "type": "geofence",
    "locationId": "loc_123",
    "distance": "450m"
  }
}

Detection Methods (In Order):
1. Check data.type == "geofence"
2. Check data.type == "geofencing"
3. Check if notification.title contains "geofence"

Any match triggers geofence handling logic.
```

## 6. Service Method Calls Diagram

```
┌──────────────────────────────────────────────────────────────┐
│              NOTIFICATION SERVICE CALL CHAIN                 │
└──────────────────────────────────────────────────────────────┘

FCM Notification Arrives
         │
         ├─ if (foreground)
         │       └─→ onMessage listener
         │           └─→ showLocalNotification()
         │
         ├─ if (background or tapped)
         │       └─→ onMessageOpenedApp listener
         │           └─→ Detect notification type
         │               └─→ if (geofence)
         │                   └─→ _handleGeofenceNotification()
         │                       │
         │                       ├─→ _isUserAuthenticated()
         │                       │
         │                       ├─ if (authenticated)
         │                       │   └─→ Get.toNamed('/geofence-status')
         │                       │
         │                       └─ else
         │                           ├─→ Get.snackbar()
         │                           └─→ Get.toNamed('/login')
         │
         └─ if (terminated)
                 └─→ getInitialMessage()
                     └─→ Check notification type
                         └─→ if (geofence)
                             └─→ _handleGeofenceNotification()
                                 └─→ (same as above)

Local Notification Tap (Foreground)
         │
         └─→ onDidReceiveNotificationResponse
             └─→ _handleLocalNotificationTap()
                 │
                 ├─→ Check if geofence
                 │
                 ├─→ _isUserAuthenticated()
                 │
                 ├─ if (authenticated)
                 │   └─→ Get.toNamed('/geofence-status')
                 │
                 └─ else
                     ├─→ Get.snackbar()
                     └─→ Get.toNamed('/login')
```

## 7. Authentication State Machine

```
                         ┌─────────────────┐
                         │ NOT LOGGED IN    │
                         │  (initial state) │
                         └────────┬─────────┘
                                  │
                       User clicks "Login"
                                  │
                                  ↓
                   ┌────────────────────────────┐
                   │ LOGGING IN                 │
                   │ (credentials being sent)   │
                   └────────┬──────────┬────────┘
                            │          │
                    Success │          │ Failure
                            │          │
                            ↓          ↓
                    ┌─────────────┐ ┌──────────────┐
                    │ LOGGED IN   │ │ AUTH FAILED  │
                    │ (active)    │ │ (error shown)│
                    └──────┬──────┘ └──────┬───────┘
                           │               │
                           │ User clicks   │ User retries
                           │ "Logout"      │ login
                           │               │
                           │               ↓
                           │          (back to LOGGING IN)
                           │
                           ↓
                    ┌──────────────────┐
                    │ LOGGING OUT      │
                    │ (session cleared)│
                    └────────┬─────────┘
                             │
                             ↓
                    ┌─────────────────┐
                    │ NOT LOGGED IN   │
                    │ (auth state: null)│
                    └─────────────────┘

Geofence Notification Received at Each State:
• NOT LOGGED IN: Route to login screen
• LOGGING IN: Wait for auth completion, then route
• LOGGED IN: Route to geofence status screen
• LOGGING OUT: Treat as NOT LOGGED IN (immediately after logout)
```

## 8. Timeline Example

```
┌─────────────────────────────────────────────────────────────────┐
│            TIMELINE: LOGGED-IN USER RECEIVES NOTIFICATION       │
└─────────────────────────────────────────────────────────────────┘

14:30:00  ┌─────────────────────────┐
          │ User opens app           │
          │ Login successful         │
          │ currentUser = User ID    │
          └────────┬────────────────┘
                   │
14:35:00  ┌────────↓────────────┐
          │ Geofence detected   │
          │ by backend          │
          │ (user near carwash) │
          └────────┬────────────┘
                   │
14:35:02  ┌────────↓──────────────┐
          │ FCM notification sent│
          │ type: "geofence"     │
          └────────┬──────────────┘
                   │
14:35:03  ┌────────↓────────────────┐
          │ Local notification   │
          │ displayed            │
          └────────┬─────────────┘
                   │
14:35:30  ┌────────↓────────────────┐
          │ 👆 User taps          │
          │ notification banner  │
          └────────┬──────────────┘
                   │
14:35:31  ┌────────↓────────────────────┐
          │ onMessage listener   │
          │ or onMessageOpenedApp│
          └────────┬──────────────────┘
                   │
14:35:32  ┌────────↓──────────────────────┐
          │ _isUserAuthenticated()  │
          │ FirebaseAuth.instance   │
          │   .currentUser != null │
          │ Returns: TRUE         │
          └────────┬───────────────────┘
                   │
14:35:33  ┌────────↓────────────────────┐
          │ Get.toNamed()         │
          │ ('/geofence-status')  │
          └────────┬───────────────────┘
                   │
14:35:34  ┌────────↓────────────────────┐
          │ GeofenceStatusScreen │
          │ Opens               │
          │ ✅ SUCCESS          │
          └────────────────────────┘
```

---

These diagrams show the complete flow, state management, and timing of the geofencing notification authentication system.
